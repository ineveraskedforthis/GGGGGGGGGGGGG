political_decisions = {

    player_settings_open = {
		picture = "starting_options"
        alert = yes
        potential = {
			NOT = { year = 1309 }            
			ai = no
			NOT = { has_country_flag = player_settings_opened }
        }

        allow = {
        }

        effect = {
			set_country_flag = player_settings_opened
			clr_country_flag = player_settings_closed
        }
		
    }

    player_settings_close = {
	picture = "starting_options"	
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
        }

        allow = {
        }

        effect = {
			set_country_flag = player_settings_closed
			clr_country_flag = player_settings_opened
        }
		
    }


    bankruptcy_events = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_country_flag = bankruptcy_events_disabled }
        }

        allow = {
        }

        effect = {
			set_country_flag = bankruptcy_events_disabled
        }
		
    }
	
    disable_sanctions = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_global_flag = sanctions_disabled }
        }

        allow = {
        }

        effect = {
			set_global_flag = sanctions_disabled
        }
		
    }
	
    enable_multiplayer = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_global_flag = multiplayer_enabled }
        }

        allow = {
        }

        effect = {
			set_global_flag = multiplayer_enabled
        }
		
    }
	
    not_interested_in_grants = {
	picture = "starting_options"
        alert = no
        potential = {
			ai = no
			has_global_flag = multiplayer_enabled
			NOT = { has_country_flag = not_interested_in_grants }
        }

        allow = {
        }

        effect = {
			set_country_flag = not_interested_in_grants
			clr_country_flag = interested_in_grants
        }
    }

    interested_in_grants = {
	picture = "starting_options"
        alert = no
        potential = {
			ai = no
			has_global_flag = multiplayer_enabled
			has_country_flag = not_interested_in_grants
        }

        allow = {
        }

        effect = {
			set_country_flag = interested_in_grants
			clr_country_flag = not_interested_in_grants
        }
    }

    request_grant = {
	picture = "starting_options"
        alert = no
        potential = {
			ai = no
			has_global_flag = multiplayer_enabled
			NOT = { has_country_flag = not_interested_in_grants }
        }

        allow = {
        }

        effect = {

			any_country = {
				limit = {
					ai = no
					has_global_flag = multiplayer_enabled
					NOT = { has_country_flag = not_interested_in_grants }
				}
				country_event = { id = 50016 }
			}
        }
    }
	
	
	
    enable_pandemics = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_global_flag = pandemics_enabled }
        }

        allow = {
        }

        effect = {
			set_global_flag = pandemics_enabled
        }
		
    }
	
    double_all_pops = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_global_flag = double_the_pops }
        }

        allow = {
        }

        effect = {
			set_global_flag = double_the_pops
			any_pop = { reduce_pop = 2 }
			any_country = {
				any_pop = { reduce_pop = 2 }
			}
        }
		
    }
	
    ai_cash_injections = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_global_flag = ai_cash_injections_initiated }
        }

        allow = {
        }

        effect = {
			set_global_flag = ai_cash_injections_initiated
			any_country = {
				limit = { ai = yes }
				treasury = 100000
				add_country_modifier = { name = ai_econ_helper duration = 36525 }
			}
        }
		
    }

    reenable_craftsmen_exploit = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_global_flag = craftsman_exploit_reenabled }
        }

        allow = {
        }

        effect = {
			set_global_flag = craftsman_exploit_reenabled
        }
		
    }
	
    infamy_decay_options = {
	picture = "starting_options"
        alert = no
        potential = {
			NOT = { year = 1309 }
			ai = no
			has_country_flag = player_settings_opened
			NOT = { has_global_flag = infamy_decay_options }
        }

        allow = {
        }

        effect = {
			set_global_flag = infamy_decay_options
			country_event = { id = 50425 } 
        }
		
    }
	
	open_encyclopedia = {
        alert = no
		picture = encycl
        potential = {
			ai = no
			NOT = { has_country_flag = encyclopedia_open }
        }

        allow = {
        }

        effect = {
		
		country_event = { id = 50117 }
		set_country_flag = encyclopedia_open

        }
    }
	
	
}	
